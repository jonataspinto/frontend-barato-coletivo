import React from 'react';
import * as S from './HeaderStyled';

const Header = () => {
  return (
    <S.HeaderContainer>
      Header works!
    </S.HeaderContainer>
  );
};

export default Header;
