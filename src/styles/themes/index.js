import light from './light';
import dark from './dark';
import usePersistedThemeState from './usePersistedThemeState';

const themes = {
  light,
  dark,
};

export { usePersistedThemeState };

export default themes;
